name: Update Shadowrocket rules

on:
  push:
    paths:
      - .github/workflows/update-rules.yml
      - scripts/generate.main.kts
      - shadowrocket.template.conf
      - shadowrocket.conf
      - rules/**
  pull_request:
    paths:
      - .github/workflows/update-rules.yml
      - scripts/generate.main.kts
      - shadowrocket.template.conf
      - shadowrocket.conf
      - rules/**
      - examples/macos-resolver/**
  schedule:
    - cron: "17 */6 * * *"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: update-rules-${{ github.ref }}
  cancel-in-progress: false

jobs:
  validate:
    if: github.event_name == 'pull_request' || github.ref != 'refs/heads/main'
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    permissions:
      contents: read

    steps:
      - name: Получить репозиторий
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4

      - name: Показать версии toolchain
        run: |
          java -version
          kotlinc -version

      - name: Сгенерировать и проверить правила
        run: kotlinc -script scripts/generate.main.kts

      - name: Проверить служебные файлы
        run: |
          test -s shadowrocket.conf
          test -s examples/macos-resolver/mdb.yandexcloud.net.example
          grep -Fxq '[Host]' shadowrocket.conf
          grep -Fxq 'nameserver VPC_DNS_IP_1' examples/macos-resolver/mdb.yandexcloud.net.example
          ! grep -Fq '{{ROSCOMVPN_' shadowrocket.conf
          ! grep '^always-real-ip = ' shadowrocket.conf | grep -Fq 'yandexcloud.net'

      - name: Проверить актуальность generated-файлов
        run: git diff --exit-code -- rules shadowrocket.conf

  update:
    if: github.event_name != 'pull_request' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    permissions:
      contents: write

    steps:
      - name: Получить репозиторий
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4

      - name: Показать версии toolchain
        run: |
          java -version
          kotlinc -version

      - name: Сгенерировать и проверить правила
        run: kotlinc -script scripts/generate.main.kts

      - name: Опубликовать изменения
        run: |
          if ! git status --porcelain -- rules shadowrocket.conf | grep -q .; then
            echo "Правила не изменились"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add rules shadowrocket.conf
          git commit -m "Обновить профиль и правила из RoscomVPN"
          git push
